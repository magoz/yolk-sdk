---
description: Manage @yolk-sdk package versioning and npm releases
---

Manage Yolk package versioning, validation, and npm publish flows.

First, invoke the skill tool to load the package-release skill:

```ts
skill({ name: 'package-release' })
```

Then follow the skill instructions.

<user-request>
$ARGUMENTS
</user-request>
