---
description: Audit and fix code against Yolk patterns
---

Audit and fix code so it conforms to Yolk repo patterns.

First, invoke the skill tool to load the conform skill:

```ts
skill({ name: 'conform' })
```

Then follow the skill instructions.

<user-request>
$ARGUMENTS
</user-request>
