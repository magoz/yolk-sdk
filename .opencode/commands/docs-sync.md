---
description: Audit and update Yolk docs after code changes
---

Audit or update Yolk docs so `apps/docs` stays aligned with package code and examples.

First, invoke the skill tool to load the docs-sync skill:

```ts
skill({ name: 'docs-sync' })
```

Then follow the skill instructions.

<user-request>
$ARGUMENTS
</user-request>
