---
'@yolk-sdk/connectors': patch
---

Add a portable generic email connector with host-provided IMAP, POP3, and SMTP transport, normalized message schemas, separate incoming and SMTP credential slots, and username/password runtime credentials.
