---
'@yolk-sdk/connectors': patch
---

Add generic IMAP and POP3 attachment retrieval through an optional host email port method, returning decoded file bytes as base64 with normalized attachment metadata while preserving existing host adapters.
