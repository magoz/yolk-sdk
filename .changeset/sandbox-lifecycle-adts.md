---
'@yolk-sdk/sandbox': minor
---

Resume expired persistent sandboxes by stable name, model Git basic auth as an all-or-nothing value, and constrain snapshot retention counts to Vercel's supported range.
