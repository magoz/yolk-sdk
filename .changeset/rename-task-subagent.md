---
'@yolk-sdk/agent': minor
---

Rename the model-facing `task` delegation tool and its public helpers to `subagent` so hosts can distinguish isolated child-agent runs from durable application tasks.
