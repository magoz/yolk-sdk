---
'@yolk-sdk/mcp': minor
---

Add full MCP `2026-07-28` client, server, and wire-schema support through the official TypeScript SDK v2, including stateless HTTP, MRTR, subscriptions, cache hints, routable headers, and dual-era legacy compatibility. Remote Yolk tool helpers now negotiate the modern protocol automatically while retaining initialize-based fallback.
