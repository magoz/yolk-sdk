---
'@yolk-sdk/harness': patch
---

Publish snapshot RunStore claims only after durable save succeeds. Tool-batch Completed now continues when tools executed; pending HITL still fences mixed execution.
