---
'@yolk-sdk/connectors': minor
---

Add Gmail label create/get/update/delete plus starring, `outlook.move_message` and flagging plus Outlook master-category list/get/create/update/delete and message category assignment, plus optional host-backed IMAP keyword label mutations (`email.modify_labels`), flagging (`email.set_flag`), and generic IMAP message relocation (`email.move`). Preserve existing email adapters and document provider-specific permissions, non-atomic Outlook merges, destination UID remapping, and POP3/SMTP limitations.
