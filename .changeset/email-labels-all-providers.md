---
'@yolk-sdk/connectors': minor
---

Add Gmail label create/get/update/delete actions, `outlook.move_message` plus Outlook master-category list/create/delete and message category assignment, plus optional host-backed IMAP keyword label mutations (`email.modify_labels`) and generic IMAP message relocation (`email.move`). Preserve existing email adapters and document provider-specific permissions, non-atomic Outlook merges, destination UID remapping, and POP3/SMTP limitations.
