---
'@yolk-sdk/connectors': minor
---

Add an official Afloat remote MCP connector contract with the canonical endpoint, required MCP protocol version, API-key credential slot, and server-side auth-data action.
