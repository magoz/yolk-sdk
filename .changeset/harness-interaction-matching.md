---
'@yolk-sdk/harness': patch
---

Match action-backed interaction responses to their original request and tool-call IDs when resuming a paused run through the harness HITL helpers. Hosts still own authentication and immutable acceptance before executing the selected action.
